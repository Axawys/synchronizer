name: sync_ui
description: Shared Flutter UI for the desktop and mobile apps — the device list and the pairing flow, so both platforms behave identically.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.12.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.0
  sync_net:
    path: ../sync_net

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
