name: sync_net
description: Networking layer shared by the desktop and mobile apps. Local-network peer discovery now, the pairing and sync transport later.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  crypto: ^3.0.3
  path: ^1.9.0
  sync_core:
    path: ../sync_core

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
