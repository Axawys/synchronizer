# Private working files, not for the public repository
ForClaude
Completed.md
todo.md

# Dart / Flutter build and tooling output
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
**/generated_plugin_registrant.dart
**/.flutter-plugins
**/.flutter-plugins-dependencies

# Per-package pubspec.lock for application-less packages is kept; app locks are
# noise across machines, so they are ignored here.
**/pubspec.lock

# Packaged release artefacts (tarballs and the trees they are built from).
dist/

# Generated localisation code: `flutter pub get` rebuilds it from the .arb
# files, so the source of truth is lib/l10n/*.arb and the output is noise.
shared/sync_ui/lib/l10n/gen/

# IDE and editor
.idea/
.vscode/
*.iml
*.ipr
*.iws
.settings/
*.swp

# OS clutter
.DS_Store
Thumbs.db

# Obsidian workspace config (this folder gets opened as a vault during testing)
.obsidian/

# Android
android/android/.gradle/
android/android/local.properties
android/android/key.properties
android/android/**/GeneratedPluginRegistrant.java
*.keystore
*.jks

# Linux desktop build
linux/linux/flutter/ephemeral/

# Symbols and logs
*.log
app.*.symbols
app.*.map.json
